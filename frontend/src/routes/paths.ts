/**
 * Centralized route path constants. Add new routes here as pages are
 * built out, and reference this object instead of hardcoding path strings.
 */
export const ROUTES = {
  home: "/",
  admin: "/admin",
} as const;
